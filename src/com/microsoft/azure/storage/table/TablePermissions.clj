(ns com.microsoft.azure.storage.table.TablePermissions
  "Represents the permissions for a table."
  (:refer-clojure :only [require comment defn ->])
  (:import [com.microsoft.azure.storage.table TablePermissions]))

(defn ->table-permissions
  "Constructor."
  (^TablePermissions []
    (new TablePermissions )))

